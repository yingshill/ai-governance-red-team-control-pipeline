# Sample report

Committed sample of what `make report` produces. Real outputs land in
`reports/` (gitignored) and are attached to the CI run as artifacts.

Generate fresh locally:

```bash
make eval
make report
open reports/index.html
```
