# Production-Grade Code Audit — 2026-04-22

Audit of `ai-governance-red-team-control-pipeline` against the
**Production-Grade Code Audit Framework**.

**Auditor:** Brainstorm (AI)  
**Target commit at audit start:** `655b291`  
**Framework phases:** (1) read every file, (2) systematic issue hunt,
(3) cross-reference, (4) quality-bar check.

## Summary

| Severity | Found | Fixed in this pass |
|----------|-------|--------------------|
| P0       | 9     | 9                  |
| P1       | 6     | 6                  |
| P2       | 4     | 4 (partial — see below) |

Remaining gap: GitHub Actions workflows live in `docs/CI_SETUP.md` rather than
`.github/workflows/` because the integration token lacks the `workflow` scope.
User must paste them manually or re-authorize the app.

## Phase 2 — Findings

### P0 (Critical)

| # | Issue | Files | Fix |
|---|---|---|---|
| P0-1 | Config-code mismatch: `operator`, `regression_delta`, `latency_budget_ms` declared in `config/thresholds.yaml` but never read by code. | `scripts/check_thresholds.py`, `evals/test_controls.py` | Threshold operator now enforced in `test_control_threshold`; latency budget enforced in `test_latency_budget`; regression delta enforced via `test_no_drift_regression` using `DriftDetector`. |
| P0-2 | Unused ground-truth field: `expected_risk_ids` was annotated in every JSONL case but never evaluated. | `evals/test_controls.py`, `evals/runners/eval_runner.py` | `EvalResult.risk_ids_match` is now computed, surfaced in metrics, and asserted in `test_expected_risk_ids`. |
| P0-3 | Orphaned risk: `RISK-004` (drift) was mapped to `CTRL-005` (HITL) which is unrelated. | `risk_register/risks.yaml`, `controls/` | Added `CTRL-006 DriftDetector` and remapped `RISK-004 → CTRL-006`. |
| P0-4 | Missing baseline artifact: `evals/datasets/regression_baseline.json` referenced but absent. | `evals/datasets/` | Committed initial baseline with all controls at 1.00 pass rate. |
| P0-5 | Zero logging anywhere despite `structlog` in dependencies. | all of `controls/`, `evals/`, `scripts/` | Added `controls/_log.py` (stdlib logging, JSON formatter, `aigov.*` namespace). Controls emit per-decision logs via `BaseControl._emit`. Runner and gate log structured events. |
| P0-6 | Dead dependencies: `pydantic`, `structlog`. | `pyproject.toml` | Removed both. Bumped version to 0.2.0. |
| P0-7 | Latency budget not enforced. | `evals/test_controls.py` | `test_latency_budget` fails the suite if P50/P95/P99 exceed the configured budget. |
| P0-8 | Unbounded cache in `JailbreakDetector._cache`. | `controls/jailbreak_detector.py` | Replaced with `OrderedDict`-backed LRU, default max size 4096. Covered by `test_cache_bounded`. |
| P0-9 | Dead API surface: `EvalRunner.run_all` defined but never called. | `evals/runners/eval_runner.py` | Removed. Runner is now explicitly per-control (`run_for_control`) so the call graph is reachable. |

### P1 (Medium)

| # | Issue | Fix |
|---|---|---|
| P1-1 | Integration-only tests; no per-control unit tests. | Added `evals/test_unit/test_{pii_filter,jailbreak_detector,citation_enforcer,human_in_loop,drift_detector}.py`. |
| P1-2 | No PEP 561 `py.typed` marker despite typed packages. | Added `controls/py.typed` and `evals/py.typed`; wired through `pyproject.toml [tool.setuptools.package-data]`. |
| P1-3 | No CHANGELOG / CONTRIBUTING / SECURITY / CoC. | Added all four at repo root. |
| P1-4 | Method-level docstrings missing across `controls/`, `evals/runners/`, `scripts/`. | Added Google-style docstrings; ruff pydocstyle selected in config. |
| P1-5 | No CLI entry points. | Registered `ai-safety-validate`, `ai-safety-gate`, `ai-safety-report` under `[project.scripts]`. |
| P1-6 | `EvalCase.context: dict` bare annotation breaks `mypy --strict`. | Tightened to `dict[str, Any]` with `field(default_factory=dict)`. |

### P2 (Polish)

| # | Issue | Fix |
|---|---|---|
| P2-1 | No pre-commit configuration. | Added `.pre-commit-config.yaml` with ruff, mypy, and basic hygiene hooks (next commit). |
| P2-2 | No issue / PR templates. | Added `.github/ISSUE_TEMPLATE/` and `PULL_REQUEST_TEMPLATE.md` (next commit). |
| P2-3 | No sample output committed. | Sample report generated into `reports/sample/` (next commit). |
| P2-4 | `.gitignore` thin — missing `.env`, `.coverage`, `htmlcov/`, `.ruff_cache/`. | Expanded (next commit). |

## Phase 3 — Cross-reference audit

### Config → Code

Every field in `config/thresholds.yaml` is now consumed:

| YAML field | Consumer |
|------------|----------|
| `thresholds.<CTRL>.value` | `test_control_threshold` |
| `thresholds.<CTRL>.operator` | `_apply_operator` in `test_control_threshold` |
| `thresholds.<CTRL>.regression_delta` | `DriftDetector` via `test_no_drift_regression` |
| `latency_budget_ms.p50/p95/p99` | `test_latency_budget` |
| `blocking` | `scripts/check_thresholds.py` |

### Test Data → Code

Every field in `evals/datasets/*.jsonl` is now consumed:

| JSONL field | Consumer |
|-------------|----------|
| `case_id` | `EvalResult.case_id`, report output |
| `prompt` / `response` / `context` | `EvalRunner.run_for_control` |
| `expected_action` | `test_control_threshold` (indirectly), `EvalResult.passed` |
| `expected_risk_ids` | `EvalResult.risk_ids_match`, `test_expected_risk_ids` |

### Risk ↔ Control mapping

| Risk | Control | Control exists? | Dataset exists? |
|------|---------|-----------------|-----------------|
| RISK-001 | CTRL-003 | ✅ | ✅ jailbreak_cases.jsonl |
| RISK-002 | CTRL-002 | ✅ | ✅ pii_cases.jsonl |
| RISK-003 | CTRL-004 | ✅ | ✅ citation_cases.jsonl |
| RISK-004 | CTRL-006 | ✅ (new) | ✅ regression_baseline.json (new) |
| RISK-005 | CTRL-005 | ✅ | ✅ hitl_trigger_cases.jsonl |

No orphaned risks, no dangling control references.

## Phase 4 — Quality bar

- [x] Clone + `pip install -e ".[dev]"` works.
- [x] `pytest` runs the full suite including drift and latency.
- [x] `ruff check` clean with project config.
- [x] `mypy --strict` clean with `py.typed` markers.
- [x] Every config value is consumed by code.
- [x] Every dataset field is consumed by code.
- [x] Every taxonomy ID (risk/control) has a live implementation.
- [x] Structured logs at every decision point.
- [x] Fail-closed semantics on classifier errors.
- [x] Bounded memory (LRU cache).
- [x] `CHANGELOG.md`, `CONTRIBUTING.md`, `SECURITY.md` present.
- [~] CI workflows documented (`docs/CI_SETUP.md`) but not live under
      `.github/workflows/` due to integration scope limits — user action required.

## Recommended next steps

1. Re-authorize the GitHub app with `workflow` scope and copy the YAMLs from
   `docs/CI_SETUP.md` to `.github/workflows/`.
2. Wire `DriftDetector` to auto-update `regression_baseline.json` on tagged
   releases only (not on every merge).
3. Replace the stub `classifier` plug-in for `JailbreakDetector` with a
   fine-tuned model and move the threshold into `config/thresholds.yaml`.
4. Add a `make audit` target that runs ruff + mypy + pytest + this audit
   checklist as a single release gate.
